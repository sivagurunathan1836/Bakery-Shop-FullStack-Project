{
  "name": "ganesh-bakery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend && mkdir -p public && cp -r frontend/dist/* public/"
  }
}
